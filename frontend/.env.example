# HoraPro frontend — variables de build (Vite las incrusta al compilar)
# Copia a .env antes de correr `npm run build`.

# URL pública del API en producción
VITE_API_URL="https://horapro.krumlab.com/api"
